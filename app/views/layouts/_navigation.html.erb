<nav class="<%= current_page?(root_path) ? 'navbar transparent' : 'navbar' %> navbar-expand-md navbar-light bg-light fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand js-scroll-trigger" href="<%= root_path %>#home"><%= image_tag('logo.png') %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="<%= root_path %>#home">Home</a>
        </li>
        <% if current_page?(root_path) %>
          <li class="nav-item">
            <a class="nav-link js-scroll-trigger" href="#search">Search</a>
          </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link js-scroll-trigger" href="<%= root_path %><%= current_page?(root_path) ? '#popular' : '#home' %>">Popular</a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item"><%= link_to current_user.username, edit_user_registration_path, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'nav-link' %></li>
        <% else %>
          <li class="nav-item"><%= link_to "Login", new_user_session_path, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to "SIGN UP", new_user_registration_path, class: 'btn btn-primary sign-up' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
